# Updated manually for GitHub Actions registration
name: "03 - K8s Cluster Config & App Deploy"

on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        description: 'Target Environment'
        required: true
        default: 'demo'
        options:
          - demo
          - test
          - prod

env:
  AWS_REGION: us-east-1
  EKS_CLUSTER_NAME: demo-eks

jobs:
  kubernetes-deployment-stage:
    name: "Stage 3: K8s Cluster Config & App Deploy"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Helm CLI
        uses: azure/setup-helm@v3
        with:
          version: 'v3.12.0'

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Step 3.1 - Update Kubeconfig
        run: |
          aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name ${{ env.EKS_CLUSTER_NAME }}

      - name: Step 3.2 - Join Worker Nodes (aws-auth)
        run: |
          NODE_ROLE_ARN="arn:aws:iam::651501331356:role/eksWorkerNodeRole"

          echo "Using NodeInstanceRole ARN: ${NODE_ROLE_ARN}"

          curl -O https://s3.us-west-2.amazonaws.com/amazon-eks/cloudformation/2020-10-29/aws-auth-cm.yaml
          sed -i "s|<ARN of instance role (not instance profile)>|${NODE_ROLE_ARN}|g" aws-auth-cm.yaml

          kubectl apply -f aws-auth-cm.yaml

          echo "=== Waiting up to 5 minutes for worker nodes to reach Ready state ==="
          kubectl wait --for=condition=Ready nodes --all --timeout=300s

          kubectl get node -o wide

      - name: Step 3.3 - Install Metrics Server, Observability & Logging Stack
        run: |
          helm repo add metrics-server https://kubernetes-sigs.github.io/metrics-server/
          helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
          helm repo add elastic https://helm.elastic.co
          helm repo add fluent https://fluent.github.io/helm-charts
          helm repo update

          helm upgrade --install metrics-server metrics-server/metrics-server \
            --namespace kube-system \
            --set args="{--kubelet-insecure-tls}"

          sleep 10

          helm upgrade --install prometheus prometheus-community/kube-prometheus-stack \
            --namespace monitoring \
            --create-namespace \
            --set grafana.enabled=true \
            --set prometheus.prometheusSpec.scrapeInterval="15s"

          helm upgrade --install elasticsearch elastic/elasticsearch \
            --namespace logging \
            --create-namespace \
            --set persistence.enabled=false \
            --set replicas=1 \
            --set minimumMasterNodes=1 \
            --set resources.requests.cpu="100m" \
            --set resources.requests.memory="512Mi" \
            --wait

          helm upgrade --install kibana elastic/kibana \
            --namespace logging \
            --create-namespace \
            --set resources.requests.cpu="100m" \
            --set resources.requests.memory="512Mi" \
            --set elasticsearchHosts="https://elasticsearch-master:9200" \
            --set createSecret=false \
            --wait

          cat <<'VALUES_EOF' > /tmp/fluent-bit-values.yaml
env:
  - name: ELASTIC_PASSWORD
    valueFrom:
      secretKeyRef:
        name: elasticsearch-master-credentials
        key: password

config:
  inputs: |
    [INPUT]
        Name tail
        Path /var/log/containers/prod-app-*_*.log
        multiline.parser docker, cri
        Tag kube.*
        Mem_Buf_Limit 10MB
        Skip_Long_Lines On

  outputs: |
    [OUTPUT]
        Name es
        Match *
        Host elasticsearch-master.logging.svc.cluster.local
        Port 9200
        HTTP_User elastic
        HTTP_Passwd ${ELASTIC_PASSWORD}
        tls On
        tls.verify Off
        Suppress_Type_Name On
        Logstash_Format On
        Logstash_Prefix fluent-bit
        Replace_Dots On
        Retry_Limit 5
        net.connect_timeout 10
VALUES_EOF

          helm upgrade --install fluent-bit fluent/fluent-bit \
            --namespace logging \
            -f /tmp/fluent-bit-values.yaml

          kubectl rollout restart daemonset/fluent-bit -n logging
          kubectl rollout status daemonset/fluent-bit -n logging --timeout=180s

      - name: Step 3.4 - Deploy Base Setup, Security Controls & Observability Resources
        run: |
          kubectl apply -f eks/k8s/base-setup.yaml
          kubectl apply -f eks/k8s/rbac.yaml
          kubectl apply -f eks/k8s/network-policy.yaml
          
          kubectl apply -f eks/k8s/observability/prometheus-rules.yaml
          kubectl apply -f eks/k8s/observability/grafana-dashboard-cm.yaml
          kubectl apply -f eks/k8s/observability/alertmanager-routing.yaml

      - name: Step 3.5 - Deploy App Workloads to Selected Environment Namespace
        run: |
          ENV_NAME="${{ github.event.inputs.environment }}"
          if [ -z "$ENV_NAME" ]; then
            ENV_NAME="demo"
          fi
          
          TARGET_NS="${ENV_NAME}-apps"
          ECR_REPO="${ENV_NAME}-app-repo"
          
          AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          ECR_IMAGE="${AWS_ACCOUNT_ID}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${ECR_REPO}:latest"

          kubectl create ns ${TARGET_NS} --dry-run=client -o yaml | kubectl apply -f -
          sed -i '/^[[:space:]]*namespace:/d' eks/k8s/app/*.yaml

          if ! aws ecr describe-images --repository-name ${ECR_REPO} --image-ids imageTag=latest --region ${{ env.AWS_REGION }} > /dev/null 2>&1; then
            echo "WARNING: ECR image not found! Falling back to nginx:alpine..."
            ECR_IMAGE="nginx:alpine"
          fi

          sed -i "s|image: nginx:latest|image: ${ECR_IMAGE}|g" eks/k8s/app/deployment.yaml

          kubectl apply -f eks/k8s/app/configmap.yaml -n ${TARGET_NS}
          kubectl apply -f eks/k8s/app/secret.yaml -n ${TARGET_NS}
          kubectl apply -f eks/k8s/app/resource-limits.yaml -n ${TARGET_NS}
          kubectl apply -f eks/k8s/app/deployment.yaml -n ${TARGET_NS}
          kubectl apply -f eks/k8s/app/service.yaml -n ${TARGET_NS}
          kubectl apply -f eks/k8s/app/hpa.yaml -n ${TARGET_NS}
          kubectl apply -f eks/k8s/app/ingress.yaml -n ${TARGET_NS}

      - name: Step 3.6 - Validate Cluster Workloads & Observability Config Status
        run: |
          ENV_NAME="${{ github.event.inputs.environment }}"
          if [ -z "$ENV_NAME" ]; then
            ENV_NAME="demo"
          fi

          echo "=== 1. Checking Application Workload Status ==="
          kubectl get pods,svc,hpa,ingress -n ${ENV_NAME}-apps

          echo "=== 2. Checking Observability Stack Status ==="
          kubectl get pods,svc,configmap -n monitoring

          echo "=== 3. Checking Logging Stack Status ==="
          kubectl get pods,svc -n logging
